# ── IRIS Core & Brain ──
google-generativeai==0.8.3
# Use a modern Groq SDK so STT/Vision works with httpx 0.28+.
# Older groq==0.12.0 could fail with: Client.__init__() got an unexpected keyword argument 'proxies'
groq>=0.20.0
# Keep httpx compatible with google-genai and newer SDKs.
httpx>=0.28.1,<1.0.0
anthropic==0.40.0
python-dotenv==1.0.1
requests==2.32.3
pytest>=8.3,<10

# ── IRIS Terminal UI ──
rich==13.9.4

# ── IRIS Voice & Audio Engine ──
edge-tts>=6.1.18
pygame==2.6.1
SpeechRecognition==3.11.0
PyAudio==0.2.14; platform_system == "Windows"
# faster-whisper and ctranslate2 removed: replaced by Groq cloud STT (whisper-large-v3-turbo).
# Keeping them adds ~300 MB to the image and introduces CUDA dep conflicts with llama-cpp-python.

# ── IRIS Hardware & VRAM Monitor ──
numpy==1.26.4
scipy>=1.11.4,<1.14
nvidia-ml-py==12.535.133

# ── IRIS v5.0 Ironclad Additions ──
duckduckgo-search>=5.0.0    # Required for tools/researcher.py (The Eyes)
psutil>=5.9.8               # Required for system/disk pre-flight checks
playwright>=1.42.0          # Required for core/browser.py automation

# ── Optional local/runtime helpers ──
sounddevice>=0.4.6
send2trash

# ── Vision (screen capture) ──
# Required by core/vision.py (_try_vision). Previously missing — would crash
# silently on "look at my screen" commands after a clean pip install.
mss>=9.0.1
Pillow>=10.0.0

# Local C++ LLM support is optional because llama-cpp-python may require MSVC/NMake
# when no compatible wheel is available. Install it separately with:
#   python -m pip install -r requirements-local-llm.txt
